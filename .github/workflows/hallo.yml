name: say hello
on: push

jobs: 
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@4
      - name: say-hello
        run: echo "hello World"
        
